import "package:flutter/material.dart";

class Profil extends StatelessWidget{
  @override
  Widget build(BuildContext context) {
    return Container(
      child: const Center(
        child: Text("Ini Tampilan Profil"),),
    );
  }
}
